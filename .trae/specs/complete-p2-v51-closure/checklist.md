- [x] 已完成代码现状审计，并形成“已实现/缺失/风险”差距清单
- [x] 队列文件按账本隔离持久化，且同日触发仅保留一个日期任务
- [x] App 重启或崩溃后，未完成任务可恢复续跑
- [x] 消费端仅处理当前账本队列，切账本后消费目标正确切换
- [x] 同日消费中重入触发不会导致任务被误删（CAS 出队保护生效）
- [x] 消费失败不会丢任务，成功后才出队
- [x] 前置改写与 dirtyDates 入队成对成立，无静默漏入队
- [x] 前置改写成功但入队失败时，补偿机制可在重启后自动补齐
- [x] AI 写回前存在 `is_verified` 二次校验，运行中锁定不会被覆盖
- [x] 日期归一化规则在筛选/入队/消费三处一致（含 23:59/00:00 边界）
- [x] 空日任务可被正常清理出队，不形成死循环
- [x] 全链路“生产→入队→消费→Arbiter写回→状态反馈”可观测并可复现
- [x] 设置页已移除 `AUTO_RULES` 废弃入口
- [x] 设置页 `MANAGE_CATEGORIES` 入口对应可用功能而非占位日志
- [x] 设置页提示语与辅助文案完成中文化收敛（按钮/标题策略例外）
- [x] 调试命令覆盖 P2 最小必测回归集并可在控制台调用
- [x] 文档已更新并明确冲突处理：实现口径以 v5.1 第 5 章为准
